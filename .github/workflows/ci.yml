# Squash CI — build + test only.
# Release pipelines, signing, and artifact packaging are later phases
# (see docs/05-architecture.md §5); this workflow intentionally stays lean.
#
# House rule: CI minutes are metered. Routine docs/session-end commits carry
# `[ci skip]` in the commit message (AGENTS.md); push jobs honor it below.
# PRs always run — `github.event.head_commit` is absent on pull_request events.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  rust:
    name: Rust (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'pull_request' || !contains(github.event.head_commit.message, '[ci skip]')
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      # Tauri v2 system deps, needed because the workspace includes the
      # Tauri host crate (app/src-tauri). Per https://v2.tauri.app/start/prerequisites/
      - name: Install Tauri Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libxdo-dev \
            patchelf

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

  frontend:
    name: Frontend (app/)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || !contains(github.event.head_commit.message, '[ci skip]')
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

  # GUI snapshots (Phase 5): pixel-compares the full screen × state ×
  # en/ar × light/dark matrix against the committed Linux baselines in
  # app/snapshots-ci/. When a scenario has no baseline yet, the run
  # bootstraps it (see the header of app/scripts/snapshots.mjs) — download
  # the artifact and commit it. macOS baselines (app/snapshots/) are the
  # local design-review surface and are not compared here.
  snapshots:
    name: GUI snapshots (ubuntu)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || !contains(github.event.head_commit.message, '[ci skip]')
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Check snapshots
        run: npm run test:snapshots:check

      # Bootstrapped baselines (new scenarios) and pixel diffs (failures).
      - name: Upload snapshot output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: snapshots-ci-output
          path: |
            app/snapshots-ci/
            app/snapshots-tmp/diff-*.png
          if-no-files-found: ignore

  # GUI E2E (Phase 5, docs/05 §6): the REAL built app driven via WebDriver —
  # @wdio/tauri-service with the embedded provider (tauri-plugin-wdio-
  # webdriver behind cargo feature `e2e`), so no external tauri-driver is
  # needed and macOS works too (WKWebView has no standalone driver). Happy
  # paths only: F1 launch → S1, F2 compress (+CLI round-trip), F3 extract,
  # RTL dir/screenshot. Details: app/e2e/README.md.
  e2e:
    name: E2E (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    if: github.event_name == 'pull_request' || !contains(github.event.head_commit.message, '[ci skip]')
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      # Same Tauri deps as the rust job, plus Xvfb for the headless runner.
      - name: Install Tauri Linux system dependencies + Xvfb
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libxdo-dev \
            patchelf \
            xvfb

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm ci

      # The runner builds frontend + app (feature e2e) + CLI itself; Linux
      # needs a virtual display for the WebKitGTK window.
      - name: Run E2E scenarios
        if: runner.os == 'Linux'
        run: xvfb-run -a npm run test:e2e

      - name: Run E2E scenarios
        if: runner.os != 'Linux'
        run: npm run test:e2e

      # Failure screenshots + the RTL visual-check pair (app/e2e/artifacts/).
      - name: Upload E2E artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts-${{ matrix.os }}
          path: app/e2e/artifacts/
          if-no-files-found: ignore

